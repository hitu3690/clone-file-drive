/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    remotePatterns: [
      {
        hostname: 'tacit-kookabura-641.convex.cloud',
      },
    ],
  },
};

export default nextConfig;
